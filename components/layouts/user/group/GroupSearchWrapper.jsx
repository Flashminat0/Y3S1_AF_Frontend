import React from 'react';

const GroupSearchWrapper = ({children}) => {
    return (
        <div className={"p-6"}>
            {children}
        </div>
    );
};

export default GroupSearchWrapper;