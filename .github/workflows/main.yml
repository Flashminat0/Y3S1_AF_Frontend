name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: CDNievas/heroku-action@v1.0 # This is the action
        with:
          heroku_email: "minato.paypal.sl@gmail.com"
          heroku_api_key: "6d558e11-91ec-4669-bfbf-4909d45bbdab"
          heroku_app_name: "sliit-reserch-system-frontend"
